import { Icon, Typography } from "@mui/material";

export default function Index() {
    return (
        <Typography variant="headlineLarge" sx={{ p: 6 }} component="p">
            Template done! Now you can start coding! <Icon>code</Icon>
        </Typography>
    );
}
